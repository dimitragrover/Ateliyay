var r = 200;

function setup(){
  createCanvas(720,480);
  frameRate(5);
}

function draw(){
  background (0,0,0);
  fill(255,20,147);
  ellipse(width/2, height/2, 65);
  
  //for( var i=0; i<30; i++){
  //fill(255,255,255);
  //noStroke();
  //ellipse(random(width), random(height), 65);

  //}
}
